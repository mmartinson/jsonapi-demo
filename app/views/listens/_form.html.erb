<%= form_for(@listen) do |f| %>
  <% if @listen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listen.errors.count, "error") %> prohibited this listen from being saved:</h2>

      <ul>
      <% @listen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.text_field :album_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
